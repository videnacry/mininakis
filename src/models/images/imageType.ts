class ImageType {
    src: string
    name: string
    date: string

    constructor (pSrc: string, pName: string, pDate: string) {
        this.src = pSrc
        this.name = pName
        this.date = pDate
    }
}

export default ImageType